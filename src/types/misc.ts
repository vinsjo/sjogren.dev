/**
 * Type representation of `width` and `height` values as a Record.
 */
export type WH = { width: number; height: number };
/**
 * Type representation of `min` and `max` values as a tuple.
 */
export type MinMax = [min: number, max: number];
